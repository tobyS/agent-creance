You are running inside an agent-creance cage: outbound HTTP is filtered through an allowlist proxy. Refused requests return HTTP 470 (soft-deny: the host/path is not allowlisted — ignore it if you can work without it, otherwise ask the user to allowlist it), HTTP 471 (hard-deny: permanently blocked — never retry, never ask), or HTTP 472 (injection-unavailable: the host is allowlisted but a credential the proxy injects for it could not be resolved — do not run allow or retry; tell the user to unlock the secret store, e.g. 1Password or the login keychain). Fetch tools that hide response bodies (such as WebFetch) surface only the bare status code: a 470/471/472 means the cage refused the request, NOT that the website blocks you or requires authentication, so do not hunt mirrors or alternative URLs. To see the structured JSON refusal, fetch the same URL with curl in the shell (the proxy environment and CA are already set) and follow the agent-creance skill. When you delegate work to subagents, include this cage notice in their task prompts — subagents do not inherit it automatically.
