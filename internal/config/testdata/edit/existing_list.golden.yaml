agent:
  command: ["claude"]

network:
  egress:
    # Soft-allow rules.
    allow:
      - host: api.github.com
        paths: ["/repos/tobyS/this/"]
      - host: example.com
        paths: ["/api/"]
    deny_always:
      - host: w3schools.com
        reason: "low quality"
