.claude-commit

# Personal Claude Code settings (shared config lives in .claude/settings.json and .claude/tce/)
.claude/settings.local.json

# Build & test artifacts
/bin/
coverage.out

# Python enforcer test venv + caches
/.venv-enforcer/
__pycache__/
.pytest_cache/

# In-cage (dogfooding) Go caches — see .agent-creance.yaml env: redirects
/.gocache/

